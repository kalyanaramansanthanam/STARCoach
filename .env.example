# Required: Gemini API key for AI coaching feedback
GOOGLE_API_KEY=your-google-api-key

# Optional: Directory to store recordings and database (default: ./data)
# STARCOACH_DATA_DIR=./data

# Optional: Host port for the web UI (default: 3000)
# STARCOACH_PORT=3000
